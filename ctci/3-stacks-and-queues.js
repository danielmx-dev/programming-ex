const assert = require('assert')
const {Node, Stack} = require('./classes')



describe('3.1', () => {
  // Write code to remove duplicates from an unsorted linked list. FOLLOW UP
  // How would you solve this problem if a temporary bu er is not allowed? 

  const something = () => {
  }

  it('does something', () => {})
})
